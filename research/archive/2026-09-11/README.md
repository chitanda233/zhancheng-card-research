# 占城大师本地原始资源归档 · 2026-09-11

本次仅复制本机现存原始内容，不解包、不解密、不执行游戏、不从服务器补下载。旧归档和微信原文件均保留。

## 内容与入口

- AppID：`wx9eed71970378b2ae`。
- `raw/packages/wx9eed71970378b2ae/16/`：当前本机最新包版本目录的全部 3 个原始 `.wxapkg` 文件，15,577,940 字节。`13/` 是较早缓存版本，已于本次复查后从归档删除，避免后续分析混用；微信原目录未改动。
- `raw/local/wx9eed71970378b2ae/`：完整游戏本地目录，100 个文件，172,439,133 字节；包含资源缓存、临时文件、私有目录、配置、图像存储及 MMKV 本地存储。保留空目录。
- `raw/icon/`：该游戏图标，1 个文件，42,163 字节。
- 原始文件合计：104 个，188,059,236 字节（约 179.35 MiB）。
- `manifest.json`：来源根目录、相对路径、大小、逐文件 SHA-256、校验时间，以及备份期间重新复制的文件清单。

## 校验与边界

2026-09-11 10:30（北京时间）完成。每个文件均在读取/必要时重新复制之后再次读取源文件，确认源内容与归档 SHA-256 一致；两个源目录的文件及子目录相对路径清单与归档一致。

微信正在运行，首次整体校验发现本地存储有变动；随后以共享只读方式读取并刷新了 4 个存储文件，逐文件校验通过。这不是全局原子快照，不保证不同 MMKV 文件之间的事务一致性。若后续研究需要恢复账号存储，应另做退出微信后的冷备份。

本归档覆盖当前已定位到的该 AppID 专属 packages、local 目录及图标。共享 codecache/data 中未找到带该 AppID 的路径，未将无法归属的其他小游戏或微信共享数据混入。未缓存的远端资源不在本次范围内，不宣称这是线上游戏的全部资源或最新版本。

包目录 `13` 与 `16` 是微信本地保存的小程序包版本目录，不是两套需要同时分析的游戏资源。`13` 的三个包创建于 2026-09-03 11:22，`16` 创建于 11:28；`16` 编号更高且写入更晚，因此归档以 `16` 为唯一研究入口。这里确认的是“当前本机所见最新”，不是对线上发布后台版本的证明。

## 交接说明

后续只从 `raw/packages/wx9eed71970378b2ae/16/` 的原始包和 `raw/local/.../usr/__GAME_FILE_CACHE` 的缓存副本开始解包；请将输出另放目录，不覆盖本归档的 raw 文件。`archive.ps1` 与 `verify.ps1` 留作本次复制/校验过程记录；不要对现有归档再次执行校验脚本，因为它会从实时源刷新不同内容。

包含账号相关本地存储，整个新归档已通过项目 `.gitignore` 排除。仅本地保存，未上传 GitHub，未经检查不要公开。
