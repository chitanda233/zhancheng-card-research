找到了，而且这次从 WASM 函数体补实了旧报告中的几处缺口。**最明确的是：5 连胜/5 连败触发标签；付费和回流 Buff 在结算链中扣次；玩家标签确实被写进战斗载荷。86 套 PVP Bot 的最终抽取函数及服务端权重仍未找到。**

以下都是本地 **2026-09-03 快照**的静态证据，不代表当前线上服务器行为。没有另写报告，直接给原始文件和调用位置。

1. **四个标签如何设置、触发、清除**

   | 标签 | 已确认的实现 |
   |---|---|
   | `WinningStreak` | `RefreshUserLableWinStreakData()`：连胜数 `+1`，连败数清零；`CurrentWinStreak > 4` 时成立。一次失败会清掉连胜标签。 |
   | `LosingStreak` | `RefreshUserLableLoseStreakData()`：连败数 `+1`，连胜数清零；`CurrentLoseStreak > 4` 时成立。一次胜利会清掉连败标签。 |
   | `JustPaidUser` | `RefreshUserLableJustNowPaidData()` 把 `JustNowPaid=true`，加入标签，并初始化付费 Buff。扣至 0 时把布尔值及标签一起清除。 |
   | `InactiveUser` | 网络时间减 `LastLoginTime` **超过 1 天**时触发，回流 Buff 补到 2。随后更新登录时间、重新检查枚举；但 getter 使用“枚举存在 **或 Buff>0**”，因此效果仍可持续。 |

   原始函数：[连胜](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[连败](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[付费置位](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[回流检测](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[回流 getter](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)。

2. **标签可否共存、SSSR 是否累加**

   **可以共存**，不是四选一。例如刚付费、连败、回流可以同时成立。但正常胜负更新链会使连胜与连败互斥。

   地块生成函数计算：

   ```text
   本方 JustPaidUser   +10
   本方 LosingStreak  +50
   本方 InactiveUser  +30
   对手 WinningStreak +50
   ```

   四项是**权重直接相加**，最后一项读取的是对手。若全部适用，增量为 `140`；基础总权重为 100 时：

   ```text
   SSSR概率 = (基础SSSR权重 + 增量) / (100 + 增量)
   ```

   不是直接加百分点。符合条件的 SSSR 兵营内容生成后，会消耗本局的标签权重加成状态；**这不等于清除账号标签或付费 Buff**，而且发生在初始化生成内容时，不必等玩家翻开。

   原始：[权重累加](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[加入抽样权重](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[生成后的消耗位置](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)。

3. **近期胜负、连胜连败、付费、回流是否参与 Bot 选择**

   **已证实参与敌方体验参数和地块质量；尚未证实参与 86 套 Bot 的身份选择。**

   本地有近期战绩、连胜连败、付费和回流状态，但没有找到“这些状态 → 某个 `PvpBotConfig.id`”的完整选择链。`PvpSettingConfig` 中还有 `M_Win_K`、`M_Win_Param`、`Bot_LoseTime`，只能确认配置存在，不能凭名称恢复匹配算法。

   原始：[玩家状态字段及存档键](research/archive/2026-09-03/reverse-engineering/dump.cs:87707)、[PVP 全局配置](tables/PvpSettingConfig.json)。

4. **玩家状态是否参与 AI difficulty 选择**

   **有明确调用。**

   ```text
   AIBehaviourManager.EnemyRandomBattleData()
     → EnmeyDifficultyLevelHandle(D)
     → RecordEnemyOpenHexTimeOffset(...)
     → GetEnemyBattleDataCofig(...)
   ```

   难度调整为：`Rookie -1`、`NormalPlayer 不变`、`Expert +1`，若有 `FirstMatchUser` 再 `-1`，最后限制到有效难度范围。

   胜率分类为 `<50`、`50～<90`、`>=90`。这里的难度档不是 PVP Bot ID；付费和回流在已核对的难度函数中没有直接加减档，而是影响开格间隔等参数。

   原始：[实际调用者](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[难度调整函数](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)。

5. **玩家状态是否进入 matchmaking 请求或匹配参数**

   需要分两条协议：

   - **`StartMatchReq` 顶层没有这四个标签**，字段为 `ArenaLevel / RulesetId / ConfigVersion / EntryModeId / ConfigHash`。
   - **战斗注册载荷包含标签**：`CreatePlayerLoadoutNormalized()` 调用 `ResolveUserLabelState()`，写入 `PlayerBattleLoadout.UserLabels`；注册链将载荷序列化并写进 `RegisterBattleInfoReq.LoadoutJson`。
   - 另一套 `MatchEnqueuePayload` 有 `DevLoadoutJson`，对应对象有 `devLoadout`，能承载这些标签；但未确认正式线上排队是否填写、服务端是否用于配对。

   因此，**“标签进入战斗上传数据”已经有证据，“服务端用标签挑对手”仍没有证据。**

   原始：[StartMatchReq](research/archive/2026-09-03/reverse-engineering/dump.cs:173798)、[标签写入载荷](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[注册序列化链](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[MatchEnqueuePayload](research/archive/2026-09-03/reverse-engineering/dump.cs:738789)。

6. **86 套 Bot 由什么函数、权重选择**

   复核是 **86 条预设记录**。已找到的候选配置是：

   ```text
   PvpRankConfig.Pvp_BotDiff → PvpBotConfig.id
   ```

   例如青铜 I 的池为 `1～10`，同一行还有：

   ```text
   Pvp_BotTime     = 15
   Pvp_BotProb     = 80
   Pvp_BotProbPlus = 20
   Pvp_BotTypeProb = 100
   ```

   **最终抽取函数、池内是否等权、概率增量什么时候累积，仍没有恢复。** 不能把普通竞技场的 `GetEnemyBattleDataCofig()` 当成这 86 条预设的选择器。

   原始：[86 条预设](tables/PvpBotConfig.json)、[段位候选池及参数](tables/PvpRankConfig.json:3)。

7. **AI difficulty 怎样影响开格、经济、攻防**

   **开格频率已经查到消费者：**

   ```text
   难度表 moveInterval
     → _openHexTimeOffset
     → Update 比较 _logicTime - _frontTime
     → EnemyOpenHex(IsHard)
   ```

   表中前六档间隔为 `15、9、3、2、1、0.5` 秒。`isPVE=false` 分支会加入：刚付费 `+5秒`、回流 `+5秒`、首局 `+5秒`。这是该 AI 分支的尝试开格间隔，实际还受暂停、可行动状态、金币和候选格限制。

   **经济与攻防要分开看：**

   - 难度表改变卡牌等级、主城等级/经济范围、神器与天赋配置。
   - 行为分支由 `aiType` 进入 `Development / Gambling / Offense`。已看到发育分支前 30 秒优先资源格；进攻分支主要优先兵营；赌博分支按随机条件优先问号格。
   - **未找到 difficulty 数字直接改变这些战术分支权重、或独立防守决策权重的证据。**
   - 本地托管 PVP Bot 另用 `LocalHostedBotCommandPlanner`。例如其回退规则明确跳过“PVP 金币门槛启用、价格25、当前金币不足200”的候选，不能套用上述旧 AI 间隔。

   原始：[难度表](tables/EnemyDifficultLevelInfosgameplay_improve_01.json)、[间隔设置](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[Update 消费](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[发育](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[进攻](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[PVP 金币门槛](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)。

8. **JustPaidUser 的充值来源、持续时间**

   找到的入口是：

   ```text
   ShopManager.PurchaseProductCallBack(productId)
     → RefreshUserLableJustNowPaidData()
     → JustNowPaid = true
     → JustPaidUser 标签置位
     → JustNowPaidGameBuff = Clamp(原值+5, 0, 5)
   ```

   存档键为 `jnp` 和 `jnpgb`。结算链调用 `RefreshOrInitJustNowPaidUserBuff(false)`，每次减 1；归零清除。再次付费补到上限 5，**不是无限累积，也没有看到按小时到期的判断**。

   可以确认是“持续 5 次扣次结算”的计数设计；不同模式是否都经过该结算分支仍需核对。**尚未追到具体充值服务器响应字段、订单状态码或补单入口。**

   原始：[充值回调调用位置](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[计数与清除](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)。

9. **BattleResult 后更新哪些状态影响下一局**

   已找到 `BattleGameManager.SettlementAnimationEnd` 的状态更新链：

   ```text
   清除当天首局状态
   → RecordPlayerNear20GameSituation(isVictory)
   → 刷新近期胜率与 Rookie/NormalPlayer/Expert
   → 回流 Buff 扣1
   → 付费 Buff 扣1
   → 按胜负更新连胜/连败及对应标签
   ```

   近期记录编码是 `0=胜、1=负`；当前函数体明确用 **胜利条目数 ÷20 ×100**，并存在初始化预填记录的分支。旧报告中“只有4条时自动按4条计算”的例子与这里不一致。

   原始：[结算调用链](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[胜率计算](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)、[首局状态清除](research/archive/2026-09-03/reverse-engineering/canonical/method-map.tsv)。

   注意：这是客户端结算动画结束入口；尚未把所有联网 `BattleResultNtf`、断线、退出和特殊模式逐一接通。

10. **普通局、连败后、付费后的原始战斗包**

   **本地没有找到能按这三类归档的真实逐局抓包**，不能拿 Bot 预设冒充实际对局。

   已找到可用于后续比对的原始协议定义：

   - `StartMatchReq`：匹配请求。
   - `RegisterBattleInfoReq.LoadoutJson`：战斗载荷，重点看 `UserLabels`。
   - `MatchSuccessNtf`：对手 ID、房间 ID、双方基础数据。
   - `BattleResultNtf`：`BaseScore / Win / Rewards`。
   - 本地玩家存档：`n2gs / rwr / cws / cls / jnp / jnpgb / iugb / ifmt`。

   原始：[注册协议](research/archive/2026-09-03/reverse-engineering/dump.cs:172568)、[匹配结果](research/archive/2026-09-03/reverse-engineering/dump.cs:171260)、[结算协议](research/archive/2026-09-03/reverse-engineering/dump.cs:166040)。

目前你指定的链路中，**Payment/结算/回流 → PlayerTag → AI参数与战斗Loadout → Hex SSSR Weight → 内容生成**已有可核对代码；缺口主要在 **服务端 Match/Bot 选择算法和真实对局包**。

<oai-mem-citation>
<citation_entries>
MEMORY.md:136-138|note=[located local game research archive and prior reports]
</citation_entries>
<rollout_ids>
01a06549-8cf1-7ca3-afb0-ebef49334fa4
</rollout_ids>
</oai-mem-citation>
